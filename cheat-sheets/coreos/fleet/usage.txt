
# show all the members of the cluster:

fleetctl list-machines

# Then load and start the unit:

fleetctl load hugo.service
fleetctl start hugo.service

# Your container has been started somewhere on the cluster. To verify the status, run:

fleetctl status hugo.service

# To stop the container, run:

fleetctl destroy hugo.service


Run a high availability service

[X-Fleet]
Conflicts=apache@*.service

The Conflicts attribute tells fleet that these two services can’t be run on the same machine, giving us high availability.

$ fleetctl start apache@1
$ fleetctl start apache@2
$ fleetctl list-units



