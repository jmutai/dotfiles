<IfModule mod_ssl.c>
        <VirtualHost _default_:443>
            ServerAdmin webmaster@localhost
            DocumentRoot /var/www/html
            ErrorLog ${APACHE_LOG_DIR}/error.log
            CustomLog ${APACHE_LOG_DIR}/access.log combined
            SSLCertificateFile      /etc/letsencrypt/live/rafikihost.co.ke/fullchain.pem
            SSLCertificateKeyFile   /etc/letsencrypt/live/rafikihost.co.ke/privkey.pem
              #        This enables optimized SSL connection renegotiation handling when SSL
                #        directives are used in per-directory context.
                #SSLOptions +FakeBasicAuth +ExportCertData +StrictRequire
                <FilesMatch "\.(cgi|shtml|phtml|php)$">
                                SSLOptions +StdEnvVars
                </FilesMatch>
                <Directory /usr/lib/cgi-bin>
                                SSLOptions +StdEnvVars
                </Directory>

       </VirtualHost>
</IfModule>





                RewriteEngine on
                RewriteCond %{REQUEST_URI} ^/\.well-known/acme-challenge/
                RewriteRule ^ - [END]
                RewriteCond %{HTTP_HOST} ^healthykenyan\.com$ [NC]
                RewriteRule ^(.*)$ http://www.healthykenyan.com( [R=301,NE,L]
                RewriteCond %{HTTPS} off
                RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI} [R=301,L]

RewriteEngine on
                RewriteCond %{REQUEST_URI} ^/\.well-known/acme-challenge/
                RewriteRule ^ - [END]
                RewriteCond %{HTTP_HOST} ^healthykenyan\.com$ [NC]
                                RewriteRule ^(.*)$ https://www.healthykenyan.com( [R=301,NE,L]
)
